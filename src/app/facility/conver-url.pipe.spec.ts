import { ConverUrlPipe } from './conver-url.pipe';

describe('ConverUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
